package app.conf;

public interface SDao {
	/*
	List<CustomMap> ds01SL(String queryId, CustomMap dbObj);
	CustomMap ds01SR(String queryId, CutomMap dbObj);
	CustomMap ds01SR(String queryId);
	
	int ds01IR(String queryId, CustomMap dbObj);
*/
}
